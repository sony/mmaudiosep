# Pretrained models

**Note:** Pretrained models are currently unavailable but are expected to be released in an upcoming update.

The expected directory structure (full version):

```bash
MMAudioSep
├── ext_weights
│   ├── best_netG.pt
│   ├── synchformer_state_dict.pth
│   ├── v1-16.pth
│   └── v1-44.pth
├── weights
│   ├── mmaudiosep_small_16k.pth
│   ├── mmaudiosep_small_44k.pth
│   ├── mmaudiosep_medium_44k.pth
│   └── mmaudiosep_large_44k.pth
└── ...
```

The expected directory structure (minimal, for the recommended model only):

```bash
MMAudioSep
├── ext_weights
│   ├── synchformer_state_dict.pth
│   └── v1-44.pth
├── weights
│   └── mmaudiosep_large_44k.pth
└── ...
```
